// This file was auto-generated by Fern from our API Definition.

import * as Darwin from "../../src/api/index";
import { DarwinClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("DealsClient", () => {
    test("listDeals (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            deals: [
                {
                    id: "id",
                    aiId: "aiId",
                    goalId: "goalId",
                    mode: "BUY",
                    direction: "demand",
                    title: "title",
                    status: "draft",
                    visibility: "PUBLIC",
                    accessPolicyId: "accessPolicyId",
                    terms: { key: "value" },
                    counterparty: { key: "value" },
                    traceId: "traceId",
                    sentAt: "2024-01-15T09:30:00Z",
                    respondedAt: "2024-01-15T09:30:00Z",
                    createdAt: "2024-01-15T09:30:00Z",
                    updatedAt: "2024-01-15T09:30:00Z",
                },
            ],
        };

        server.mockEndpoint().get("/deals").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.deals.listDeals();
        expect(response).toEqual(rawResponseBody);
    });

    test("listDeals (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.listDeals();
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("listDeals (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.listDeals();
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("listDeals (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.listDeals();
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("listDeals (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.listDeals();
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("createDeal (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { mode: "BUY", title: "title" };
        const rawResponseBody = {
            deal: {
                id: "id",
                aiId: "aiId",
                goalId: "goalId",
                mode: "BUY",
                direction: "demand",
                title: "title",
                status: "draft",
                visibility: "PUBLIC",
                accessPolicyId: "accessPolicyId",
                terms: { key: "value" },
                counterparty: { key: "value" },
                traceId: "traceId",
                sentAt: "2024-01-15T09:30:00Z",
                respondedAt: "2024-01-15T09:30:00Z",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .post("/deals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.deals.createDeal({
            mode: "BUY",
            title: "title",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createDeal (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { mode: "BUY", title: "title" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.createDeal({
                mode: "BUY",
                title: "title",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("createDeal (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { mode: "BUY", title: "title" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.createDeal({
                mode: "BUY",
                title: "title",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("createDeal (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { mode: "BUY", title: "title" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.createDeal({
                mode: "BUY",
                title: "title",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("createDeal (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { mode: "BUY", title: "title" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.createDeal({
                mode: "BUY",
                title: "title",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("getDeal (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            deal: {
                id: "id",
                aiId: "aiId",
                goalId: "goalId",
                mode: "BUY",
                direction: "demand",
                title: "title",
                status: "draft",
                visibility: "PUBLIC",
                accessPolicyId: "accessPolicyId",
                terms: { key: "value" },
                counterparty: { key: "value" },
                traceId: "traceId",
                sentAt: "2024-01-15T09:30:00Z",
                respondedAt: "2024-01-15T09:30:00Z",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server.mockEndpoint().get("/deals/dealId").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.deals.getDeal({
            dealId: "dealId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getDeal (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals/dealId").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.getDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("getDeal (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals/dealId").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.getDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("getDeal (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals/dealId").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.getDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("getDeal (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/deals/dealId").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.deals.getDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("updateDeal (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {
            deal: {
                id: "id",
                aiId: "aiId",
                goalId: "goalId",
                mode: "BUY",
                direction: "demand",
                title: "title",
                status: "draft",
                visibility: "PUBLIC",
                accessPolicyId: "accessPolicyId",
                terms: { key: "value" },
                counterparty: { key: "value" },
                traceId: "traceId",
                sentAt: "2024-01-15T09:30:00Z",
                respondedAt: "2024-01-15T09:30:00Z",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .patch("/deals/dealId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.deals.updateDeal({
            dealId: "dealId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("updateDeal (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .patch("/deals/dealId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.updateDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("updateDeal (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .patch("/deals/dealId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.updateDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("updateDeal (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .patch("/deals/dealId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.updateDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("updateDeal (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .patch("/deals/dealId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.updateDeal({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("actOnDeal (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "SEND" };
        const rawResponseBody = {
            deal: {
                id: "id",
                aiId: "aiId",
                goalId: "goalId",
                mode: "BUY",
                direction: "demand",
                title: "title",
                status: "draft",
                visibility: "PUBLIC",
                accessPolicyId: "accessPolicyId",
                terms: { key: "value" },
                counterparty: { key: "value" },
                traceId: "traceId",
                sentAt: "2024-01-15T09:30:00Z",
                respondedAt: "2024-01-15T09:30:00Z",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .post("/deals/dealId/actions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.deals.actOnDeal({
            dealId: "dealId",
            action: "SEND",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("actOnDeal (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "SEND" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals/dealId/actions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.actOnDeal({
                dealId: "dealId",
                action: "SEND",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("actOnDeal (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "SEND" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals/dealId/actions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.actOnDeal({
                dealId: "dealId",
                action: "SEND",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("actOnDeal (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "SEND" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals/dealId/actions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.actOnDeal({
                dealId: "dealId",
                action: "SEND",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("actOnDeal (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "SEND" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/deals/dealId/actions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.actOnDeal({
                dealId: "dealId",
                action: "SEND",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("listDealPayments (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            payments: [
                {
                    id: "id",
                    aiId: "aiId",
                    goalId: "goalId",
                    dealId: "dealId",
                    amountMinor: 1,
                    currency: "currency",
                    description: "description",
                    status: "status",
                    checkoutUrl: "checkoutUrl",
                    expiresAt: "2024-01-15T09:30:00Z",
                    paidAt: "2024-01-15T09:30:00Z",
                    canceledAt: "2024-01-15T09:30:00Z",
                    metadata: { key: "value" },
                    payout: {
                        id: "id",
                        amountMinor: 1,
                        currency: "currency",
                        status: "status",
                        createdAt: "2024-01-15T09:30:00Z",
                    },
                    createdAt: "2024-01-15T09:30:00Z",
                    updatedAt: "2024-01-15T09:30:00Z",
                },
            ],
        };

        server
            .mockEndpoint()
            .get("/deals/dealId/payments")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.deals.listDealPayments({
            dealId: "dealId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("listDealPayments (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/deals/dealId/payments")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.listDealPayments({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("listDealPayments (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/deals/dealId/payments")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.listDealPayments({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("listDealPayments (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/deals/dealId/payments")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.listDealPayments({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("listDealPayments (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/deals/dealId/payments")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deals.listDealPayments({
                dealId: "dealId",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });
});
