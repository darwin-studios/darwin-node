// This file was auto-generated by Fern from our API Definition.

import * as Darwin from "../../src/api/index";
import { DarwinClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("RequestsClient", () => {
    test("listRequests (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            requests: [
                {
                    id: "id",
                    aiId: "aiId",
                    mode: "BUY",
                    intent: "BUY",
                    role: "role",
                    status: "PENDING",
                    title: "title",
                    message: "message",
                    senderAi: { key: "value" },
                    recipientAi: { key: "value" },
                    listings: [{ key: "value" }],
                    proposedDealTemplateKey: "proposedDealTemplateKey",
                    proposedTerms: { key: "value" },
                    proposedSteps: [{ key: "value" }],
                    matchReason: "matchReason",
                    compatibleGoals: [{ key: "value" }],
                    receivedAt: "2024-01-15T09:30:00Z",
                    respondedAt: "2024-01-15T09:30:00Z",
                    createdAt: "2024-01-15T09:30:00Z",
                    updatedAt: "2024-01-15T09:30:00Z",
                },
            ],
        };

        server.mockEndpoint().get("/requests").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.requests.listRequests();
        expect(response).toEqual(rawResponseBody);
    });

    test("listRequests (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/requests").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.requests.listRequests();
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("listRequests (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/requests").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.requests.listRequests();
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("listRequests (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/requests").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.requests.listRequests();
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("listRequests (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/requests").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.requests.listRequests();
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("actOnRequest (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "ACCEPT" };
        const rawResponseBody = {
            request: {
                id: "id",
                aiId: "aiId",
                mode: "BUY",
                intent: "BUY",
                role: "role",
                status: "PENDING",
                title: "title",
                message: "message",
                senderAi: { key: "value" },
                recipientAi: { key: "value" },
                listings: [{ key: "value" }],
                proposedDealTemplateKey: "proposedDealTemplateKey",
                proposedTerms: { key: "value" },
                proposedSteps: [{ key: "value" }],
                matchReason: "matchReason",
                compatibleGoals: [{ key: "value" }],
                receivedAt: "2024-01-15T09:30:00Z",
                respondedAt: "2024-01-15T09:30:00Z",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .post("/requests/requestId/actions")
            .header("Idempotency-Key", "Idempotency-Key")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.requests.actOnRequest({
            "Idempotency-Key": "Idempotency-Key",
            requestId: "requestId",
            action: "ACCEPT",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("actOnRequest (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "ACCEPT" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/requests/requestId/actions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.requests.actOnRequest({
                "Idempotency-Key": "idempotencyKey",
                requestId: "requestId",
                action: "ACCEPT",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("actOnRequest (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "ACCEPT" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/requests/requestId/actions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.requests.actOnRequest({
                "Idempotency-Key": "idempotencyKey",
                requestId: "requestId",
                action: "ACCEPT",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("actOnRequest (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "ACCEPT" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/requests/requestId/actions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.requests.actOnRequest({
                "Idempotency-Key": "idempotencyKey",
                requestId: "requestId",
                action: "ACCEPT",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("actOnRequest (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { action: "ACCEPT" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/requests/requestId/actions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.requests.actOnRequest({
                "Idempotency-Key": "idempotencyKey",
                requestId: "requestId",
                action: "ACCEPT",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });
});
