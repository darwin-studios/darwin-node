// This file was auto-generated by Fern from our API Definition.

import * as Darwin from "../../src/api/index";
import { DarwinClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("DeploymentClient", () => {
    test("getAIDeployment (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            deployment: {
                aiId: "aiId",
                current: "DARWIN_CLOUD",
                options: [{ key: "value" }],
                requests: [
                    {
                        id: "id",
                        aiId: "aiId",
                        target: "DARWIN_CLOUD",
                        status: "status",
                        createdAt: "2024-01-15T09:30:00Z",
                        updatedAt: "2024-01-15T09:30:00Z",
                    },
                ],
            },
        };

        server
            .mockEndpoint()
            .get("/ais/aiId/deployment")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.deployment.getAiDeployment({
            aiId: "aiId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getAIDeployment (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/ais/aiId/deployment")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.getAiDeployment({
                aiId: "aiId",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("getAIDeployment (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/ais/aiId/deployment")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.getAiDeployment({
                aiId: "aiId",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("getAIDeployment (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/ais/aiId/deployment")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.getAiDeployment({
                aiId: "aiId",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("getAIDeployment (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/ais/aiId/deployment")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.getAiDeployment({
                aiId: "aiId",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });

    test("createAIDeploymentRequest (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target: "DARWIN_CLOUD" };
        const rawResponseBody = {
            deploymentRequest: {
                id: "id",
                aiId: "aiId",
                target: "DARWIN_CLOUD",
                status: "status",
                reason: "reason",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .post("/ais/aiId/deployment-requests")
            .header("Idempotency-Key", "Idempotency-Key")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.deployment.createAiDeploymentRequest({
            "Idempotency-Key": "Idempotency-Key",
            aiId: "aiId",
            target: "DARWIN_CLOUD",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createAIDeploymentRequest (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target: "DARWIN_CLOUD" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/ais/aiId/deployment-requests")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.createAiDeploymentRequest({
                "Idempotency-Key": "idempotencyKey",
                aiId: "aiId",
                target: "DARWIN_CLOUD",
            });
        }).rejects.toThrow(Darwin.BadRequestError);
    });

    test("createAIDeploymentRequest (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target: "DARWIN_CLOUD" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/ais/aiId/deployment-requests")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.createAiDeploymentRequest({
                "Idempotency-Key": "idempotencyKey",
                aiId: "aiId",
                target: "DARWIN_CLOUD",
            });
        }).rejects.toThrow(Darwin.UnauthorizedError);
    });

    test("createAIDeploymentRequest (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target: "DARWIN_CLOUD" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/ais/aiId/deployment-requests")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.createAiDeploymentRequest({
                "Idempotency-Key": "idempotencyKey",
                aiId: "aiId",
                target: "DARWIN_CLOUD",
            });
        }).rejects.toThrow(Darwin.ForbiddenError);
    });

    test("createAIDeploymentRequest (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new DarwinClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target: "DARWIN_CLOUD" };
        const rawResponseBody = {};

        server
            .mockEndpoint()
            .post("/ais/aiId/deployment-requests")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.deployment.createAiDeploymentRequest({
                "Idempotency-Key": "idempotencyKey",
                aiId: "aiId",
                target: "DARWIN_CLOUD",
            });
        }).rejects.toThrow(Darwin.NotFoundError);
    });
});
